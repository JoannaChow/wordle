import { observer } from "mobx-react";
import React from "react";

@observer
export class Keyboard extends React.Component {
    render() {
        return (
            <>
            </>
        );
    }
}