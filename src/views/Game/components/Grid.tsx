import { observer } from "mobx-react";
import React from "react";

@observer
export class Grid extends React.Component {
    render() {
        return (
            <>
            </>
        );
    }
}